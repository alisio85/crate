# Changelog

## 0.1.0

- Initial release.
- `no_std` dependency-free primitives:
  - logging (`LogSink`, `Logger`, level macros)
  - fixed buffers (`RingBuffer`, `FixedStr`)
  - status codes (`Status`, `KResult`)
  - hexdump helpers
  - panic helpers
